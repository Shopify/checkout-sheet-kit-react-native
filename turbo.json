{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "lint": {
      "dependsOn": ["build"]
    },
    "build": {
      "outputs": ["modules/checkout-sheet-kit/lib/**/*"]
    },
    "build:android": {
      "dependsOn": ["build", "lint"],
      "outputs": ["sample/android/app/build"]
    },
    "build:ios": {
      "dependsOn": ["build", "lint"],
      "outputs": ["sample/ios/build"]
    }
  }
}
